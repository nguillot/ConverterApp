# encoding: utf-8

When /^I enter (\d+) mile\(s\)$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^I press the 'Convert miles' button$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^I should have (\d+) km$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

Then /^I should have (\d+)\.(\d+) km$/ do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end
